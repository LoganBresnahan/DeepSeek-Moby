/**
 * HeaderShadowActor styles
 *
 * Minimal version - this actor uses light DOM and updates existing elements,
 * so no Shadow DOM styles are needed. This file is kept for consistency
 * with other actors and potential future use.
 */
export const headerShadowStyles = `
/* No styles needed - actor updates existing light DOM elements */
`;
