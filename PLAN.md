1. search capability with tavily account. requires an api key. will be attached to the global search icon. when active we utilize the tavily api with search requests from the users prompt. Do we need to be smart about this? When the user clicks the global search button it is green for active also when clicked a modal tooltip pops up with options for the api. one i know for a fact would be a slider for how many searches should be used per prompt request. also there is a toggle for basic vs advanced search where it is 1 and 2 credits per search respectivly. stats provided by tavily should display in a section in our stats modal.

2. Improve diff apply logic. Also work towards auto edit mode. we can have the current mode which is just diff and apply buttons from code snippets in the chat. we could have a second mode where the user is asked about each file that should be updated and finally and auto mode where it just updates the file without human intervention. I wonder if we need to send deepseek a better prompt for these more advanced editing modes. meaning we say something when attempting to edit a file make sure to copy or read in the entire contents so that it can rewrite the entire file with it's changes (I realize this would be largely up to the performance of the LLM but I imagine deepseek will have a smarter llm to be used with their api soon)

3. github actions process for deploying an updated version of this extension to the vscode store.