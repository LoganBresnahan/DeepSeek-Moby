# MCP (Model Context Protocol)

## Implementation status (as of 2026-06-16)

**Status: not-started — empty placeholder, no plan content and no code.**

This file was created as a stub (`docs/plans/mcp.md`, 0 bytes) and never written. There is no design here yet.

Not yet / differs:
- No MCP design or requirements have been written in this doc.
- No implementation in code: a repo-wide search for `mcp` / `Model Context Protocol` / `modelcontextprotocol` returns zero matches in `src/` (`grep -rin "mcp" src/` → 0 hits) and nothing in `package.json` (no MCP settings or commands).
- The extension's external integrations today are the web-search providers under `src/clients/*` and the in-process tool layer (`src/tools/*`); none speak MCP.

If MCP work begins, replace this section with the actual design (server discovery/launch, tool registration, transport, approval/security model) and update the status accordingly.
